{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/{your_binary_name}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {},
            "preLaunchTask": "build",
            "sourceLanguages": ["rust"]
        }
    ]
}
